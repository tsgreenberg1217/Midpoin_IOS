import React from 'react'
import {Text} from 'react-native'

const nextPage = () =>{
  return(
    <Text style = {{fontSize: 40, padding: 30}}>
    You are on the next page
    </Text>
  )
}

export default nextPage
